[gd_scene load_steps=4 format=3 uid="uid://bckrh0oeviq4m"]

[ext_resource type="Script" uid="uid://bm5a1dk1qc713" path="res://scripts/battle/BattleWindow.gd" id="1_battle"]

[sub_resource type="StyleBoxFlat" id="StyleBoxFlat_5btk3"]
bg_color = Color(0, 0, 0, 1)
border_width_left = 2
border_width_top = 2
border_width_right = 2
border_width_bottom = 2

[sub_resource type="Shader" id="Shader_mother2"]
code = "shader_type canvas_item;

uniform float time : hint_range(0.0, 100.0) = 0.0;
uniform int effect_type : hint_range(0, 7) = 0;
uniform vec3 color1 : source_color = vec3(0.2, 0.1, 0.3);
uniform vec3 color2 : source_color = vec3(0.4, 0.2, 0.5);

void fragment() {
    vec2 uv = UV;
    vec3 col = vec3(0.0);

    if (effect_type == 0) {
        // 체크무늬 스크롤
        vec2 checker = floor((uv + vec2(time * 0.1, time * 0.05)) * 8.0);
        float pattern = mod(checker.x + checker.y, 2.0);
        col = mix(color1, color2, pattern);
    }
    else if (effect_type == 1) {
        // 물결 패턴
        float wave = sin(uv.x * 10.0 + time * 2.0) * 0.5 + 0.5;
        wave *= sin(uv.y * 8.0 + time * 1.5) * 0.5 + 0.5;
        col = mix(color1, color2, wave);
    }
    else if (effect_type == 2) {
        // 방사형 패턴
        vec2 center = uv - 0.5;
        float dist = length(center);
        float angle = atan(center.y, center.x);
        float spiral = sin(dist * 20.0 - time * 3.0 + angle * 4.0) * 0.5 + 0.5;
        col = mix(color1, color2, spiral);
    }
    else if (effect_type == 3) {
        // 다이아몬드 패턴
        vec2 diamond = abs(fract((uv + vec2(time * 0.1)) * 6.0) - 0.5);
        float pattern = step(diamond.x + diamond.y, 0.5);
        col = mix(color1, color2, pattern);
    }
    else if (effect_type == 4) {
        // 노이즈 스크롤
        vec2 nuv = uv * 10.0 + time;
        float noise = fract(sin(dot(floor(nuv), vec2(12.9898, 78.233))) * 43758.5453);
        col = mix(color1, color2, noise);
    }
    else if (effect_type == 5) {
        // 수직 스트라이프
        float stripe = step(0.5, fract((uv.x + time * 0.2) * 10.0));
        col = mix(color1, color2, stripe);
    }
    else if (effect_type == 6) {
        // 동심원
        vec2 center = uv - 0.5;
        float dist = length(center);
        float rings = sin(dist * 30.0 - time * 4.0) * 0.5 + 0.5;
        col = mix(color1, color2, rings);
    }
    else {
        // 플라즈마
        float plasma = sin(uv.x * 10.0 + time);
        plasma += sin(uv.y * 10.0 + time * 1.2);
        plasma += sin((uv.x + uv.y) * 10.0 + time * 0.8);
        plasma = plasma / 3.0 * 0.5 + 0.5;
        col = mix(color1, color2, plasma);
    }

    COLOR = vec4(col, 1.0);
}
"

[node name="BattleWindow" type="PanelContainer" unique_id=1167346498]
custom_minimum_size = Vector2(280, 210)
offset_left = 3.0
offset_top = 1.0
offset_right = 283.0
offset_bottom = 211.0
mouse_filter = 0
script = ExtResource("1_battle")

[node name="MainVBox" type="VBoxContainer" parent="." unique_id=482878077]
layout_mode = 2
mouse_filter = 1
theme_override_constants/separation = 0

[node name="TopBar" type="HBoxContainer" parent="MainVBox" unique_id=1913141494]
layout_mode = 2
custom_minimum_size = Vector2(0, 22)
theme_override_constants/separation = 6

[node name="CloseButton" type="Button" parent="MainVBox/TopBar" unique_id=262349809]
visible = false
custom_minimum_size = Vector2(24, 24)
layout_mode = 2
text = "x"

[node name="BattleArea" type="PanelContainer" parent="MainVBox" unique_id=897254730]
layout_mode = 2
mouse_filter = 1
size_flags_vertical = 3
size_flags_stretch_ratio = 7.0
theme_override_styles/panel = SubResource("StyleBoxFlat_5btk3")

[node name="Background" type="ColorRect" parent="MainVBox/BattleArea"]
layout_mode = 2
size_flags_vertical = 3
mouse_filter = 2

[node name="EnemyContainer" type="VBoxContainer" parent="MainVBox/BattleArea" unique_id=197219225]
layout_mode = 2
mouse_filter = 2
size_flags_horizontal = 4
size_flags_vertical = 4
theme_override_constants/separation = 5
alignment = 1

[node name="InfoBar" type="PanelContainer" parent="MainVBox"]
layout_mode = 2
custom_minimum_size = Vector2(0, 20)

[node name="InfoLabel" type="Label" parent="MainVBox/InfoBar"]
layout_mode = 2
text = "💰0  📦0"
horizontal_alignment = 1
vertical_alignment = 1

[node name="ButtonBar" type="HBoxContainer" parent="MainVBox"]
layout_mode = 2
custom_minimum_size = Vector2(0, 30)
alignment = 1
theme_override_constants/separation = 14

[node name="RunButton" type="Button" parent="MainVBox/ButtonBar" unique_id=219318160]
unique_name_in_owner = true
custom_minimum_size = Vector2(104, 24)
layout_mode = 2
size_flags_horizontal = 4
mouse_filter = 0
tooltip_text = "즉시 도주 (보상 없음)"
theme_override_font_sizes/font_size = 9
text = "도주"
